import { SafeAreaView, Text } from 'react-native';

const DriverScreen = () => {
  return (
    <SafeAreaView>
      <Text>Driver Screen</Text>
    </SafeAreaView>
  );
};

export default DriverScreen;
